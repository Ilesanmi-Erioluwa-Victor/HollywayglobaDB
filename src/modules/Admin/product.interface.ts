export interface createProductI  {
    title: string;
    slug: string;
        description: string
        price,
        quantity,
        images,
        brand,
        stock,
        colors,
        sold,
        categoryId,
}
